# dicta v3 — Voz de salida: Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dicta lee en voz alta los avisos de Claude Code y el cierre de cada respuesta con TTS local (Kokoro ONNX), y abre el micrófono solo cuando lo leído es una pregunta.

**Architecture:** Hooks `Notification`/`Stop` de Claude Code escriben JSON en una cola de archivos (`%APPDATA%\dicta\speak\`); un QTimer en dicta la drena, un módulo de texto puro extrae el último párrafo y decide si es pregunta, `speaker.py` sintetiza con kokoro-onnx y reproduce por sounddevice, y la máquina de estados gana un estado `SPEAKING` que al terminar puede abrir escucha manos libres. Spec: `docs/superpowers/specs/2026-08-12-dicta-v3-voz-design.md`.

**Tech Stack:** Python 3.11+, PyQt6, kokoro-onnx (nuevo), sounddevice, pytest, PowerShell 5.1 para hooks.

## Global Constraints

- 100% local, sin cuentas ni API keys (filosofía de dicta).
- El flujo de dictado actual NUNCA se rompe: si la voz falla, dicta sigue como v2.
- Voz Kokoro `ef_dora`, idioma `es`, tope de cierre 400 caracteres (todos configurables en `[voz]`).
- Los avisos de permisos (`permission_prompt`) se leen pero JAMÁS abren el micrófono.
- El dictado en curso nunca se interrumpe por un mensaje de voz: espera en cola.
- Textos y comentarios de código en español, como el resto del repo.
- Rama de trabajo: `feature/v3-voz`. Commits pequeños por tarea.
- Correr tests con: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/ -v` desde `X:\Proyectos\dicta`.

---

### Task 1: Texto hablable (`speech_text.py`)

Funciones puras: extraer el último párrafo con tope por frase, limpiar markdown para TTS y detectar si un texto termina en pregunta.

**Files:**
- Create: `dicta/speech_text.py`
- Test: `tests/test_speech_text.py`

**Interfaces:**
- Produces: `ultimo_parrafo(text: str, max_chars: int = 400) -> str` · `es_pregunta(text: str) -> bool`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_speech_text.py
from dicta.speech_text import es_pregunta, ultimo_parrafo


def test_un_solo_parrafo_corto_sale_tal_cual():
    assert ultimo_parrafo("Hecho, todo verde.") == "Hecho, todo verde."


def test_devuelve_el_ultimo_parrafo():
    text = "Primero analicé el bug.\n\nLuego lo arreglé.\n\n¿Quieres que haga commit?"
    assert ultimo_parrafo(text) == "¿Quieres que haga commit?"


def test_ignora_parrafos_de_solo_codigo():
    text = "¿Lanzo los tests?\n\n```bash\npytest -v\n```"
    assert ultimo_parrafo(text) == "¿Lanzo los tests?"


def test_limpia_markdown_para_tts():
    text = "El **fix** está en `app.py`.\n\n## Resumen\nTodo *verde*."
    assert ultimo_parrafo(text) == "Resumen Todo verde."


def test_texto_vacio_devuelve_vacio():
    assert ultimo_parrafo("") == ""
    assert ultimo_parrafo("\n\n  \n") == ""


def test_tope_corta_por_frase_conservando_el_final():
    frases = [f"Frase de relleno número {i}." for i in range(30)]
    text = " ".join(frases) + " ¿Te parece bien?"
    out = ultimo_parrafo(text, max_chars=60)
    assert out.endswith("¿Te parece bien?")
    assert len(out) <= 60


def test_frase_unica_mas_larga_que_el_tope_se_corta_por_el_final():
    text = "x" * 500 + " final"
    out = ultimo_parrafo(text, max_chars=50)
    assert len(out) <= 50
    assert out.endswith("final")


def test_es_pregunta_con_signo_de_apertura():
    assert es_pregunta("Listo. ¿Hago push?") is True


def test_es_pregunta_con_signo_final():
    assert es_pregunta("Todo listo. Dime como seguimos?") is True


def test_no_es_pregunta():
    assert es_pregunta("Hecho. Todo comiteado y en verde.") is False


def test_pregunta_antigua_no_cuenta():
    # La pregunta está a más de dos frases del final: ya no es "la" pregunta.
    assert es_pregunta("¿Ves el error? Da igual. Lo arreglé. Quedó en verde.") is False


def test_es_pregunta_texto_vacio():
    assert es_pregunta("") is False
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speech_text.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'dicta.speech_text'`

- [ ] **Step 3: Write the implementation**

```python
# dicta/speech_text.py
"""Texto puro para la voz de salida: qué se lee y si acaba en pregunta.
El hook Stop entrega la respuesta completa; aquí se reduce a su último
párrafo hablable (sin bloques de código ni marcas de markdown) con un
tope de caracteres que corta por frase conservando el final, que es
donde Claude pregunta."""
import re

_FRASE = r"(?<=[.!?…])\s+"


def _limpiar(text: str) -> str:
    """Markdown fuera: un TTS no debe leer asteriscos ni fences."""
    text = re.sub(r"```.*?```", "", text, flags=re.S)
    text = re.sub(r"^\s*[#>\-*]+\s*", "", text, flags=re.M)
    text = re.sub(r"[*_`]+", "", text)
    return re.sub(r"\s+", " ", text).strip()


def ultimo_parrafo(text: str, max_chars: int = 400) -> str:
    parrafos = [p for p in re.split(r"\n\s*\n", text or "")]
    limpios = [c for c in (_limpiar(p) for p in parrafos) if c]
    if not limpios:
        return ""
    ultimo = limpios[-1]
    if len(ultimo) <= max_chars:
        return ultimo
    frases = re.split(_FRASE, ultimo)
    out: list[str] = []
    total = 0
    for f in reversed(frases):
        if out and total + len(f) + 1 > max_chars:
            break
        out.insert(0, f)
        total += len(f) + 1
    result = " ".join(out)
    return result[-max_chars:] if len(result) > max_chars else result


def es_pregunta(text: str) -> bool:
    """True si alguna de las dos últimas frases contiene interrogación."""
    frases = re.split(_FRASE, (text or "").strip())
    return any("?" in f or "¿" in f for f in frases[-2:])
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speech_text.py -v`
Expected: PASS (12 tests)

- [ ] **Step 5: Commit**

```bash
git add dicta/speech_text.py tests/test_speech_text.py
git commit -m "feat: extraccion del ultimo parrafo hablable y deteccion de pregunta"
```

---

### Task 2: Cola de habla (`speak_queue.py`)

Drenar la cola de archivos que escriben los hooks y decidir qué se habla y con qué comportamiento (coalescing + filtros de config).

**Files:**
- Create: `dicta/speak_queue.py`
- Test: `tests/test_speak_queue.py`

**Interfaces:**
- Consumes: `ultimo_parrafo`, `es_pregunta` (Task 1).
- Produces: `SpeakItem(kind: str, text: str)` · `drenar_cola(speak_dir: Path) -> list[SpeakItem]` · `preparar(items, max_chars: int, leer_avisos: bool, leer_cierres: bool, escuchar_tras_pregunta: bool) -> list[tuple[str, bool]]` (texto a hablar, abre_mic)

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_speak_queue.py
import json

from dicta.speak_queue import SpeakItem, drenar_cola, preparar


def _escribir(tmp_path, nombre, kind, text):
    (tmp_path / nombre).write_text(
        json.dumps({"ts": 1, "kind": kind, "text": text}), encoding="utf-8"
    )


def test_dir_inexistente_devuelve_vacio(tmp_path):
    assert drenar_cola(tmp_path / "no-existe") == []


def test_lee_en_orden_y_borra(tmp_path):
    _escribir(tmp_path, "001-aviso.json", "aviso", "primero")
    _escribir(tmp_path, "002-permiso.json", "permiso", "segundo")
    items = drenar_cola(tmp_path)
    assert items == [SpeakItem("aviso", "primero"), SpeakItem("permiso", "segundo")]
    assert list(tmp_path.glob("*.json")) == []


def test_coalescing_solo_el_ultimo_cierre(tmp_path):
    _escribir(tmp_path, "001-cierre.json", "cierre", "cierre viejo")
    _escribir(tmp_path, "002-aviso.json", "aviso", "un aviso")
    _escribir(tmp_path, "003-cierre.json", "cierre", "cierre nuevo")
    items = drenar_cola(tmp_path)
    assert items == [SpeakItem("aviso", "un aviso"), SpeakItem("cierre", "cierre nuevo")]


def test_corrupto_se_borra_y_se_sigue(tmp_path):
    (tmp_path / "001-cierre.json").write_text("{esto no es json", encoding="utf-8")
    _escribir(tmp_path, "002-aviso.json", "aviso", "válido")
    assert drenar_cola(tmp_path) == [SpeakItem("aviso", "válido")]
    assert list(tmp_path.glob("*.json")) == []


def test_texto_vacio_y_kind_desconocido_se_ignoran(tmp_path):
    _escribir(tmp_path, "001-aviso.json", "aviso", "   ")
    _escribir(tmp_path, "002-raro.json", "otra-cosa", "hola")
    assert drenar_cola(tmp_path) == []


def test_preparar_cierre_con_pregunta_abre_mic():
    items = [SpeakItem("cierre", "Hecho.\n\n¿Sigo con el resto?")]
    assert preparar(items, 400, True, True, True) == [("¿Sigo con el resto?", True)]


def test_preparar_cierre_sin_pregunta_no_abre_mic():
    items = [SpeakItem("cierre", "Hecho. Todo en verde.")]
    assert preparar(items, 400, True, True, True) == [("Hecho. Todo en verde.", False)]


def test_preparar_permiso_nunca_abre_mic():
    items = [SpeakItem("permiso", "Claude necesita permiso para usar Bash")]
    assert preparar(items, 400, True, True, True) == [
        ("Claude necesita permiso para usar Bash", False)
    ]


def test_preparar_aviso_abre_mic():
    items = [SpeakItem("aviso", "Claude está esperando tu respuesta")]
    assert preparar(items, 400, True, True, True) == [
        ("Claude está esperando tu respuesta", True)
    ]


def test_preparar_respeta_escuchar_tras_pregunta_off():
    # preparar conserva el orden de entrada; solo cambia abre_mic
    items = [SpeakItem("cierre", "¿Sigo?"), SpeakItem("aviso", "esperando")]
    assert preparar(items, 400, True, True, False) == [
        ("¿Sigo?", False), ("esperando", False)
    ]


def test_preparar_filtra_por_config():
    items = [SpeakItem("aviso", "un aviso"), SpeakItem("cierre", "un cierre")]
    assert preparar(items, 400, False, True, True) == [("un cierre", False)]
    assert preparar(items, 400, True, False, True) == [("un aviso", True)]


def test_preparar_aplica_tope_al_cierre():
    largo = "Relleno inicial que sobra. " * 30 + "¿Te vale así?"
    items = [SpeakItem("cierre", largo)]
    (texto, abre_mic), = preparar(items, 60, True, True, True)
    assert len(texto) <= 60
    assert texto.endswith("¿Te vale así?")
    assert abre_mic is True
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speak_queue.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'dicta.speak_queue'`

- [ ] **Step 3: Write the implementation**

```python
# dicta/speak_queue.py
"""Cola de habla: los hooks de Claude Code dejan archivos JSON en
%APPDATA%\\dicta\\speak\\ y aquí se drenan y traducen a (texto, abre_mic).
Coalescing: si se acumulan cierres atrasados solo se habla el último;
los avisos/permisos se conservan todos, en orden."""
import json
from dataclasses import dataclass
from pathlib import Path

from dicta.speech_text import es_pregunta, ultimo_parrafo

KINDS = ("aviso", "permiso", "cierre")


@dataclass
class SpeakItem:
    kind: str
    text: str


def drenar_cola(speak_dir: Path) -> list[SpeakItem]:
    """Lee por orden de nombre y borra SIEMPRE (también corruptos)."""
    if not speak_dir.is_dir():
        return []
    items: list[SpeakItem] = []
    for f in sorted(speak_dir.glob("*.json")):
        try:
            # utf-8-sig: Set-Content -Encoding utf8 de PowerShell 5.1 mete BOM
            data = json.loads(f.read_text(encoding="utf-8-sig"))
            kind = str(data.get("kind", ""))
            text = str(data.get("text", "")).strip()
            if kind in KINDS and text:
                items.append(SpeakItem(kind, text))
        except Exception:
            pass
        finally:
            f.unlink(missing_ok=True)
    cierres = [i for i in items if i.kind == "cierre"]
    otros = [i for i in items if i.kind != "cierre"]
    return otros + cierres[-1:]


def preparar(
    items: list[SpeakItem],
    max_chars: int,
    leer_avisos: bool,
    leer_cierres: bool,
    escuchar_tras_pregunta: bool,
) -> list[tuple[str, bool]]:
    """(texto a hablar, abre_mic) por item. Permisos JAMÁS abren mic."""
    out: list[tuple[str, bool]] = []
    for it in items:
        if it.kind == "cierre":
            if not leer_cierres:
                continue
            texto = ultimo_parrafo(it.text, max_chars)
            if texto:
                out.append((texto, escuchar_tras_pregunta and es_pregunta(texto)))
        elif it.kind == "aviso":
            if leer_avisos:
                out.append((it.text, escuchar_tras_pregunta))
        else:  # "permiso"
            if leer_avisos:
                out.append((it.text, False))
    return out
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speak_queue.py -v`
Expected: PASS (12 tests)

- [ ] **Step 5: Commit**

```bash
git add dicta/speak_queue.py tests/test_speak_queue.py
git commit -m "feat: cola de habla con coalescing y filtros de config"
```

---

### Task 3: Estado `SPEAKING` (`state.py`)

**Files:**
- Modify: `dicta/state.py`
- Test: `tests/test_state.py` (añadir al final)

**Interfaces:**
- Produces: `State.SPEAKING` · `StateMachine.speak_request(abre_mic: bool) -> bool` · `StateMachine.speak_done() -> None` · lista de callbacks `StateMachine.on_stop_speaking` · atributo `speak_abre_mic: bool`. `click()` en `SPEAKING` corta la voz y abre escucha.

- [ ] **Step 1: Write the failing tests**

Añadir al final de `tests/test_state.py`:

```python
# --- v3: voz de salida ---

def make_sm_voz():
    sm = StateMachine(handsfree_enabled=True)
    events = []
    sm.on_change.append(lambda s: events.append(s))
    sm.on_start_listening.append(lambda: events.append("START_REC"))
    sm.on_stop_speaking.append(lambda: events.append("STOP_SPEAK"))
    sm.model_ready()
    events.clear()
    return sm, events


def test_speak_request_desde_armed():
    sm, _ = make_sm_voz()
    assert sm.speak_request(abre_mic=False) is True
    assert sm.state is State.SPEAKING


def test_speak_request_desde_idle():
    sm = StateMachine(handsfree_enabled=False)
    sm.model_ready()
    assert sm.speak_request(abre_mic=False) is True
    assert sm.state is State.SPEAKING


def test_speak_request_rechazado_si_dictando():
    sm, _ = make_sm_voz()
    sm.wake_detected()
    assert sm.speak_request(abre_mic=False) is False
    assert sm.state is State.LISTENING


def test_speak_request_rechazado_si_transcribiendo():
    sm, _ = make_sm_voz()
    sm.wake_detected()
    sm.silence_detected()
    assert sm.speak_request(abre_mic=True) is False
    assert sm.state is State.TRANSCRIBING


def test_speak_done_sin_pregunta_vuelve_a_reposo():
    sm, events = make_sm_voz()
    sm.speak_request(abre_mic=False)
    sm.speak_done()
    assert sm.state is State.ARMED
    assert "START_REC" not in events


def test_speak_done_con_pregunta_abre_escucha_manos_libres():
    sm, events = make_sm_voz()
    sm.speak_request(abre_mic=True)
    sm.speak_done()
    assert sm.state is State.LISTENING
    assert sm.session_handsfree is True
    assert "START_REC" in events


def test_click_durante_speaking_corta_y_abre_escucha():
    sm, events = make_sm_voz()
    sm.speak_request(abre_mic=False)
    events.clear()
    sm.click()
    assert events == ["STOP_SPEAK", State.LISTENING, "START_REC"]
    assert sm.session_handsfree is True


def test_speak_done_tras_click_es_noop():
    # El stop() del speaker dispara on_done igualmente; no debe romper nada.
    sm, _ = make_sm_voz()
    sm.speak_request(abre_mic=True)
    sm.click()
    sm.speak_done()
    assert sm.state is State.LISTENING


def test_wake_ignorado_durante_speaking():
    sm, _ = make_sm_voz()
    sm.speak_request(abre_mic=False)
    sm.wake_detected()
    assert sm.state is State.SPEAKING


def test_fail_durante_speaking_va_a_error():
    sm, _ = make_sm_voz()
    sm.speak_request(abre_mic=False)
    sm.fail()
    assert sm.state is State.ERROR
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_state.py -v`
Expected: los tests nuevos FAIL (`AttributeError: 'StateMachine' object has no attribute 'on_stop_speaking'` / `speak_request`); los 16 antiguos PASS.

- [ ] **Step 3: Write the implementation**

En `dicta/state.py`:

1. Añadir a `State`:

```python
    SPEAKING = auto()
```

2. En `StateMachine.__init__`, junto a los otros callbacks:

```python
        self.speak_abre_mic = False
        self.on_stop_speaking: list[Callable[[], None]] = []
```

3. Métodos nuevos (tras `wake_detected`):

```python
    def speak_request(self, abre_mic: bool) -> bool:
        """True si dicta pasa a hablar YA; False = el llamante lo deja en cola.
        Solo se habla desde reposo: un dictado en curso nunca se interrumpe."""
        if self.state in (State.IDLE, State.ARMED):
            self.speak_abre_mic = abre_mic
            self._set(State.SPEAKING)
            return True
        return False

    def speak_done(self) -> None:
        """Fin de la reproducción. Con abre_mic la conversación sigue sola."""
        if self.state is not State.SPEAKING:
            return  # p. ej. un click ya cortó y abrió escucha
        if self.speak_abre_mic:
            self.session_handsfree = True
            self._set(State.LISTENING)
            self._dispatch(self.on_start_listening)
        else:
            self._set(self._resting())
```

4. En `click()`, nueva rama antes del comentario final:

```python
        elif self.state is State.SPEAKING:
            self._dispatch(self.on_stop_speaking)
            self.session_handsfree = True
            self._set(State.LISTENING)
            self._dispatch(self.on_start_listening)
```

5. Actualizar el comentario final de `click()` a `# LOADING y TRANSCRIBING ignoran clicks` (sin cambios) y el docstring del módulo mencionando `SPEAKING`.

- [ ] **Step 4: Run tests to verify they pass**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_state.py -v`
Expected: PASS (26 tests)

- [ ] **Step 5: Commit**

```bash
git add dicta/state.py tests/test_state.py
git commit -m "feat: estado SPEAKING con auto-escucha tras pregunta"
```

---

### Task 4: Config `[voz]` (`config.py` + `config.example.toml`)

**Files:**
- Modify: `dicta/config.py`
- Modify: `config.example.toml`
- Test: `tests/test_config.py` (añadir al final)

**Interfaces:**
- Produces: campos de `Config`: `voz_activada: bool = True`, `voz_nombre: str = "ef_dora"`, `voz_velocidad: float = 1.0`, `voz_max_caracteres: int = 400`, `leer_avisos: bool = True`, `leer_cierres: bool = True`, `escuchar_tras_pregunta: bool = True`.

- [ ] **Step 1: Write the failing tests**

Añadir al final de `tests/test_config.py` (seguir el estilo del archivo: si usa `tmp_path` + `load_config(path)`, replicarlo):

```python
def test_voz_defaults_sin_seccion(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text("[whisper]\nmodel = 'small'\n", encoding="utf-8")
    cfg = load_config(p)
    assert cfg.voz_activada is True
    assert cfg.voz_nombre == "ef_dora"
    assert cfg.voz_velocidad == 1.0
    assert cfg.voz_max_caracteres == 400
    assert cfg.leer_avisos is True
    assert cfg.leer_cierres is True
    assert cfg.escuchar_tras_pregunta is True


def test_voz_seccion_completa(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text(
        "[voz]\nactivado = false\nvoz = 'em_alex'\nvelocidad = 1.2\n"
        "max_caracteres = 250\nleer_avisos = false\nleer_cierres = false\n"
        "escuchar_tras_pregunta = false\n",
        encoding="utf-8",
    )
    cfg = load_config(p)
    assert cfg.voz_activada is False
    assert cfg.voz_nombre == "em_alex"
    assert cfg.voz_velocidad == 1.2
    assert cfg.voz_max_caracteres == 250
    assert cfg.leer_avisos is False
    assert cfg.leer_cierres is False
    assert cfg.escuchar_tras_pregunta is False
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_config.py -v`
Expected: los 2 nuevos FAIL con `AttributeError`; los antiguos PASS.

- [ ] **Step 3: Write the implementation**

En `dicta/config.py`, añadir al final del dataclass `Config`:

```python
    voz_activada: bool = True
    voz_nombre: str = "ef_dora"
    voz_velocidad: float = 1.0
    voz_max_caracteres: int = 400
    leer_avisos: bool = True
    leer_cierres: bool = True
    escuchar_tras_pregunta: bool = True
```

En `load_config`, junto a las otras secciones:

```python
    voz = data.get("voz", {})
```

y tras los asignados de `manos_libres`:

```python
    cfg.voz_activada = bool(voz.get("activado", cfg.voz_activada))
    cfg.voz_nombre = str(voz.get("voz", cfg.voz_nombre))
    cfg.voz_velocidad = float(voz.get("velocidad", cfg.voz_velocidad))
    cfg.voz_max_caracteres = int(voz.get("max_caracteres", cfg.voz_max_caracteres))
    cfg.leer_avisos = bool(voz.get("leer_avisos", cfg.leer_avisos))
    cfg.leer_cierres = bool(voz.get("leer_cierres", cfg.leer_cierres))
    cfg.escuchar_tras_pregunta = bool(
        voz.get("escuchar_tras_pregunta", cfg.escuchar_tras_pregunta)
    )
```

En `config.example.toml`, añadir al final:

```toml

[voz]
activado = true
voz = "ef_dora"           # voz de Kokoro (español: ef_dora, em_alex, em_santa)
velocidad = 1.0
max_caracteres = 400      # tope del cierre leído en voz alta
leer_avisos = true        # permisos y "esperando input"
leer_cierres = true       # último párrafo de cada respuesta
escuchar_tras_pregunta = true  # abrir el mic solo si lo leído es una pregunta
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add dicta/config.py config.example.toml tests/test_config.py
git commit -m "feat: seccion [voz] en la config"
```

---

### Task 5: Widget — estilo `SPEAKING` y check "Voz" (`widget.py`)

**Files:**
- Modify: `dicta/widget.py`
- Test: `tests/test_widget_logic.py` (añadir al final)

**Interfaces:**
- Consumes: `State.SPEAKING` (Task 3).
- Produces: señal `voice_toggled = pyqtSignal(bool)` · `set_voice(enabled: bool) -> None`. El estado `SPEAKING` pinta cápsula marfil con barras tinta en modo `bars-live` (reutiliza `set_level`).

- [ ] **Step 1: Write the failing test**

Añadir al final de `tests/test_widget_logic.py` (seguir el patrón de imports del archivo; si instancia `DictaWidget` necesita `QApplication`, seguir el patrón existente — si el archivo solo testea helpers puros, testear los dicts de estilo):

```python
def test_speaking_tiene_estilo_y_tooltip():
    from dicta.state import State
    from dicta.widget import _STYLES, _TIPS, PILL_W, _INK, _IVORY

    bg, fg, mode, width = _STYLES[State.SPEAKING]
    assert (bg, fg) == (_IVORY, _INK)
    assert mode == "bars-live"
    assert width == PILL_W
    assert State.SPEAKING in _TIPS
```

- [ ] **Step 2: Run test to verify it fails**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_widget_logic.py -v`
Expected: el nuevo FAIL con `KeyError: <State.SPEAKING...>` (o AttributeError); los antiguos PASS.

- [ ] **Step 3: Write the implementation**

En `dicta/widget.py`:

1. `_STYLES`, añadir (cápsula marfil + barras tinta: inconfundible con LISTENING, que es terracota):

```python
    State.SPEAKING: (_IVORY, _INK, "bars-live", PILL_W),
```

2. `_TIPS`, añadir:

```python
    State.SPEAKING: "Claude hablando… click para contestar",
```

3. Señal y estado interno (junto a `handsfree_toggled` y `_handsfree`):

```python
    voice_toggled = pyqtSignal(bool)
```

```python
        self._voice = True
```

4. Método junto a `set_handsfree`:

```python
    def set_voice(self, enabled: bool) -> None:
        self._voice = enabled
```

5. En `contextMenuEvent`, entre "Manos libres" y el separador:

```python
        voz = menu.addAction("Voz")
        voz.setCheckable(True)
        voz.setChecked(self._voice)
        voz.toggled.connect(self.voice_toggled.emit)
```

(replicar el patrón exacto de `hf`: `setCheckable(True)` — mirar las líneas existentes 314-318.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_widget_logic.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add dicta/widget.py tests/test_widget_logic.py
git commit -m "feat: estilo SPEAKING y check Voz en el menu del widget"
```

---

### Task 6: Motor de voz (`speaker.py` + dependencia `kokoro-onnx`)

Orquestación testeable (hilo + stop + callbacks) separada del motor real (Kokoro), que se inyecta.

**Files:**
- Create: `dicta/speaker.py`
- Modify: `pyproject.toml` (dependencia `kokoro-onnx>=0.4`)
- Test: `tests/test_speaker.py`

**Interfaces:**
- Consumes: nada interno (módulo hoja).
- Produces: `Speaker(synth, player, on_level, on_done)` con `speak(text: str) -> bool` y `stop() -> None` · `ensure_model(models_dir: Path) -> tuple[Path, Path]` · `crear_speaker(cfg, models_dir: Path, on_level, on_done) -> Speaker` (construye el Kokoro real; es lo único que importa kokoro_onnx).
  - `synth: Callable[[str], tuple[np.ndarray, int]]` (samples float32, sample_rate)
  - `player: Callable[[np.ndarray, int, threading.Event, Callable[[float], None]], None]`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_speaker.py
import threading
import time

import numpy as np

from dicta.speaker import Speaker


def _synth_ok(text):
    return np.zeros(2048, dtype=np.float32), 24000


def test_speak_sintetiza_reproduce_y_avisa_al_terminar():
    llamadas = {}
    done = threading.Event()

    def player(samples, sr, stop, on_level):
        llamadas["played"] = (len(samples), sr)
        on_level(0.2)

    niveles = []
    sp = Speaker(_synth_ok, player, niveles.append, done.set)
    assert sp.speak("hola") is True
    assert done.wait(2.0)
    assert llamadas["played"] == (2048, 24000)
    assert niveles == [0.2]


def test_speak_rechaza_si_ya_esta_hablando():
    empezo = threading.Event()
    suelta = threading.Event()

    def player(samples, sr, stop, on_level):
        empezo.set()
        suelta.wait(2.0)

    sp = Speaker(_synth_ok, player, lambda l: None, lambda: None)
    assert sp.speak("uno") is True
    assert empezo.wait(2.0)
    assert sp.speak("dos") is False
    suelta.set()


def test_stop_corta_y_on_done_se_dispara_igualmente():
    done = threading.Event()

    def player(samples, sr, stop, on_level):
        # reproduce "por bloques" hasta que stop se active
        for _ in range(100):
            if stop.is_set():
                return
            time.sleep(0.01)

    sp = Speaker(_synth_ok, player, lambda l: None, done.set)
    sp.speak("largo")
    time.sleep(0.05)
    sp.stop()
    assert done.wait(2.0)


def test_error_de_sintesis_dispara_on_done_sin_reventar():
    done = threading.Event()

    def synth_roto(text):
        raise RuntimeError("kokoro caput")

    sp = Speaker(synth_roto, lambda *a: None, lambda l: None, done.set)
    assert sp.speak("hola") is True
    assert done.wait(2.0)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speaker.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'dicta.speaker'`

- [ ] **Step 3: Write the implementation**

```python
# dicta/speaker.py
"""Voz de salida. Speaker orquesta (hilo, stop, callbacks) con un motor
inyectable; crear_speaker construye el real (Kokoro ONNX, voz española)
y es el único sitio que importa kokoro_onnx. Si el modelo no está, se
descarga la primera vez (~310 MB) como los de Whisper y Vosk."""
import sys
import threading
import urllib.request
from pathlib import Path

import numpy as np

_BASE = "https://github.com/thewh1teagle/kokoro-onnx/releases/download/model-files-v1.0"
MODEL_URL = f"{_BASE}/kokoro-v1.0.onnx"
VOICES_URL = f"{_BASE}/voices-v1.0.bin"
BLOCK = 1024


def ensure_model(models_dir: Path) -> tuple[Path, Path]:
    """Devuelve (modelo, voces), descargándolos la primera vez (~310 MB)."""
    kdir = models_dir / "kokoro"
    kdir.mkdir(parents=True, exist_ok=True)
    rutas = []
    for url in (MODEL_URL, VOICES_URL):
        destino = kdir / url.rsplit("/", 1)[1]
        if not destino.exists():
            print(f"Descargando {destino.name}…")
            parcial = destino.with_suffix(destino.suffix + ".part")
            urllib.request.urlretrieve(url, parcial)
            parcial.rename(destino)
        rutas.append(destino)
    return rutas[0], rutas[1]


def _play(samples: np.ndarray, sr: int, stop: threading.Event, on_level) -> None:
    """Reproduce por bloques para poder cortar y publicar el nivel (RMS)."""
    import sounddevice as sd  # import perezoso

    with sd.OutputStream(samplerate=sr, channels=1, dtype="float32") as out:
        for i in range(0, len(samples), BLOCK):
            if stop.is_set():
                return
            chunk = np.ascontiguousarray(samples[i : i + BLOCK], dtype=np.float32)
            out.write(chunk)
            on_level(float(np.sqrt(np.mean(chunk**2))))


class Speaker:
    """speak() sintetiza y reproduce en un hilo; stop() corta. on_done se
    dispara SIEMPRE (fin normal, corte o error): la máquina de estados
    depende de ello para salir de SPEAKING."""

    def __init__(self, synth, player, on_level, on_done) -> None:
        self._synth = synth
        self._player = player
        self._on_level = on_level
        self._on_done = on_done
        self._stop = threading.Event()
        self._thread: threading.Thread | None = None

    def speak(self, text: str) -> bool:
        if self._thread is not None and self._thread.is_alive():
            return False
        self._stop.clear()

        def work() -> None:
            try:
                samples, sr = self._synth(text)
                if not self._stop.is_set():
                    self._player(samples, sr, self._stop, self._on_level)
            except Exception as exc:
                print(f"Error de voz: {exc}", file=sys.stderr)
            finally:
                self._on_done()

        self._thread = threading.Thread(target=work, daemon=True)
        self._thread.start()
        return True

    def stop(self) -> None:
        self._stop.set()


def crear_speaker(cfg, models_dir: Path, on_level, on_done) -> Speaker:
    """Construye el Speaker real con Kokoro ONNX (única función que lo importa)."""
    from kokoro_onnx import Kokoro  # import perezoso

    model, voices = ensure_model(models_dir)
    kokoro = Kokoro(str(model), str(voices))

    def synth(text: str):
        return kokoro.create(
            text, voice=cfg.voz_nombre, speed=cfg.voz_velocidad, lang="es"
        )

    return Speaker(synth, _play, on_level, on_done)
```

En `pyproject.toml`, añadir a `dependencies`:

```toml
    "kokoro-onnx>=0.4",
```

- [ ] **Step 4: Install the dependency and run tests**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pip install -e ".[dev]"`
Expected: instala `kokoro-onnx` y sus deps (onnxruntime, espeakng-loader…) sin errores.

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/test_speaker.py -v`
Expected: PASS (4 tests — no tocan hardware ni descargan nada: synth y player son fakes)

- [ ] **Step 5: Verificación real de la voz (una vez, manual)**

Run:
```
X:\Proyectos\dicta\.venv\Scripts\python.exe -c "from types import SimpleNamespace; from pathlib import Path; import os, threading; from dicta.speaker import crear_speaker; e=threading.Event(); cfg=SimpleNamespace(voz_nombre='ef_dora', voz_velocidad=1.0); s=crear_speaker(cfg, Path(os.environ['APPDATA'])/'dicta'/'models', lambda l: None, e.set); s.speak('Hola Robert, la voz de dicta ya funciona. ¿Se me entiende bien?'); e.wait(60)"
```
Expected: descarga el modelo la primera vez (~310 MB con progreso… tarda) y **se oye la frase por los altavoces en español**. Si `lang="es"` fallara con esta versión de kokoro-onnx, probar `"es-419"` o consultar `Kokoro.get_languages()`; dejar en el código el que funcione.

**ALTO si la voz suena mal:** avisar a Robert antes de seguir (el plan B es Piper y cambia esta task, no las demás).

- [ ] **Step 6: Commit**

```bash
git add dicta/speaker.py tests/test_speaker.py pyproject.toml
git commit -m "feat: motor de voz Kokoro ONNX con reproduccion cortable"
```

---

### Task 7: Hooks de Claude Code (`notification.ps1` + `stop.ps1`)

**Files:**
- Create: `hooks/notification.ps1`
- Create: `hooks/stop.ps1`

**Interfaces:**
- Consumes: JSON por stdin de Claude Code (`Notification`: `notification_type`, `message` · `Stop`: `last_assistant_message`, `transcript_path`).
- Produces: archivos `<ts>-<kind>.json` con `{ts, kind, text}` en `%APPDATA%\dicta\speak\` (los que lee `drenar_cola`, Task 2). Kinds: `permiso` (permission_prompt), `aviso` (idle_prompt, agent_needs_input), `cierre` (Stop).

- [ ] **Step 1: Write `hooks/notification.ps1`**

```powershell
# Lee la notificacion de Claude Code por stdin y encola el texto para que
# dicta lo hable. Solo pasan los tipos utiles; el resto (auth, MCP) se ignora.
# Nunca bloquea ni falla: sin voz no hay dictado roto.
$ErrorActionPreference = "SilentlyContinue"
try {
    $p = [Console]::In.ReadToEnd() | ConvertFrom-Json
} catch { exit 0 }
$kinds = @{
    "permission_prompt" = "permiso"
    "idle_prompt"       = "aviso"
    "agent_needs_input" = "aviso"
}
$kind = $kinds[[string]$p.notification_type]
$text = [string]$p.message
if (-not $kind -or -not $text) { exit 0 }
$dir = Join-Path $env:APPDATA "dicta\speak"
New-Item -ItemType Directory -Force $dir | Out-Null
$ts = [DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds()
@{ ts = $ts; kind = $kind; text = $text } | ConvertTo-Json -Compress |
    Set-Content -Encoding utf8 (Join-Path $dir "$ts-$kind.json")
exit 0
```

- [ ] **Step 2: Write `hooks/stop.ps1`**

```powershell
# Al terminar cada respuesta de Claude, encola el texto para que dicta lea
# el cierre. Usa last_assistant_message; si el CLI no lo trae (versiones
# viejas), cae al transcript JSONL. Nunca bloquea ni falla.
$ErrorActionPreference = "SilentlyContinue"
try {
    $p = [Console]::In.ReadToEnd() | ConvertFrom-Json
} catch { exit 0 }
$text = [string]$p.last_assistant_message
if (-not $text -and $p.transcript_path -and (Test-Path $p.transcript_path)) {
    $lines = @(Get-Content $p.transcript_path -Tail 200)
    for ($i = $lines.Count - 1; $i -ge 0; $i--) {
        try { $o = $lines[$i] | ConvertFrom-Json } catch { continue }
        if ($o.type -eq "assistant" -and $o.message.content) {
            $text = ($o.message.content | Where-Object { $_.type -eq "text" } |
                ForEach-Object { $_.text }) -join "`n"
            if ($text) { break }
        }
    }
}
if (-not $text) { exit 0 }
# dicta solo lee el ultimo parrafo: con la cola del texto sobra.
if ($text.Length -gt 4000) { $text = $text.Substring($text.Length - 4000) }
$dir = Join-Path $env:APPDATA "dicta\speak"
New-Item -ItemType Directory -Force $dir | Out-Null
$ts = [DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds()
@{ ts = $ts; kind = "cierre"; text = $text } | ConvertTo-Json -Compress |
    Set-Content -Encoding utf8 (Join-Path $dir "$ts-cierre.json")
exit 0
```

- [ ] **Step 3: Smoke test de ambos hooks**

Run (PowerShell):
```powershell
'{"notification_type":"permission_prompt","message":"Claude necesita permiso para usar Bash"}' | powershell -NoProfile -File X:\Proyectos\dicta\hooks\notification.ps1
'{"notification_type":"auth_success","message":"no debe encolarse"}' | powershell -NoProfile -File X:\Proyectos\dicta\hooks\notification.ps1
'{"last_assistant_message":"Primer parrafo.\n\nHecho. ¿Sigo con el resto?"}' | powershell -NoProfile -File X:\Proyectos\dicta\hooks\stop.ps1
Get-ChildItem (Join-Path $env:APPDATA "dicta\speak") | ForEach-Object { Get-Content $_.FullName }
```
Expected: exactamente 2 archivos (`*-permiso.json` con el mensaje de Bash y `*-cierre.json` con el texto completo); el de `auth_success` NO existe. Borrar los archivos tras verificar:
```powershell
Remove-Item (Join-Path $env:APPDATA "dicta\speak\*.json")
```

- [ ] **Step 4: Commit**

```bash
git add hooks/notification.ps1 hooks/stop.ps1
git commit -m "feat: hooks Notification/Stop que encolan texto para la voz"
```

---

### Task 8: Cableado en `app.py`

**Files:**
- Modify: `dicta/app.py`

**Interfaces:**
- Consumes: `drenar_cola`/`preparar` (Task 2), `speak_request`/`speak_done`/`on_stop_speaking` (Task 3), config `[voz]` (Task 4), `set_voice`/`voice_toggled` (Task 5), `crear_speaker` (Task 6).
- Produces: dicta habla. Señales nuevas en `Bridge`: `speak_done = pyqtSignal()`, `speak_level = pyqtSignal(float)`.

- [ ] **Step 1: Imports y constantes**

En `dicta/app.py`, añadir a los imports:

```python
from dicta.speak_queue import drenar_cola, preparar
```

y junto a `STATE_FILE`/`MODELS_DIR`:

```python
SPEAK_DIR = APP_DIR / "speak"
```

- [ ] **Step 2: Señales del Bridge**

En la clase `Bridge`, añadir:

```python
    speak_done = pyqtSignal()
    speak_level = pyqtSignal(float)
    voz_failed = pyqtSignal()
```

- [ ] **Step 3: Bloque de voz en `main()`**

Insertar tras el bloque `--- wake word ---` (después de `launch_wakeword_load()` inicial) y antes de `--- carga del modelo en background ---`:

```python
    # --- voz de salida (v3): cola de los hooks -> Kokoro ---
    voz_holder: dict = {}                       # {"s": Speaker} cuando cargue
    pendientes: list[tuple[str, bool]] = []     # (texto, abre_mic)
    voz_on = {"v": cfg.voz_activada}

    def load_voz() -> None:
        try:
            from dicta.speaker import crear_speaker

            voz_holder["s"] = crear_speaker(
                cfg, MODELS_DIR, bridge.speak_level.emit,
                bridge.speak_done.emit,
            )
            print("Voz lista.")
        except Exception as exc:
            print(f"Voz no disponible: {exc}", file=sys.stderr)
            bridge.voz_failed.emit()

    if cfg.voz_activada:
        threading.Thread(target=load_voz, daemon=True).start()

    def on_voz_failed() -> None:
        voz_on["v"] = False
        widget.set_voice(False)

    bridge.voz_failed.connect(on_voz_failed)

    def intentar_hablar() -> None:
        if not pendientes:
            return
        if not voz_on["v"]:
            pendientes.clear()  # muteado o sin motor: la cola no debe crecer
            return
        if "s" not in voz_holder:
            return  # aún cargando; el coalescing mantiene la cola corta
        texto, abre_mic = pendientes[0]
        if sm.speak_request(abre_mic):
            pendientes.pop(0)
            voz_holder["s"].speak(texto)

    def revisar_cola() -> None:
        items = drenar_cola(SPEAK_DIR)
        if items:
            pendientes.extend(
                preparar(
                    items, cfg.voz_max_caracteres, cfg.leer_avisos,
                    cfg.leer_cierres, cfg.escuchar_tras_pregunta,
                )
            )
        intentar_hablar()

    speak_timer = QTimer()
    speak_timer.timeout.connect(revisar_cola)
    speak_timer.start(500)

    bridge.speak_done.connect(sm.speak_done)
    bridge.speak_level.connect(widget.set_level)
    sm.on_stop_speaking.append(
        lambda: voz_holder["s"].stop() if "s" in voz_holder else None
    )

    def reintentar_pendientes(state: State) -> None:
        if state in (State.IDLE, State.ARMED) and pendientes:
            QTimer.singleShot(0, intentar_hablar)

    sm.on_change.append(reintentar_pendientes)

    def on_voice_toggled(enabled: bool) -> None:
        voz_on["v"] = enabled
        if not enabled and "s" in voz_holder:
            voz_holder["s"].stop()  # si estaba hablando, silencio inmediato

    widget.voice_toggled.connect(on_voice_toggled)
    widget.set_voice(cfg.voz_activada)
```

Nota: `State` ya está importado en `app.py` (línea 19). `sounds.play("start")` del inicio de escucha ya suena vía `start_recording` cuando `speak_done` abre el mic — el ding de "te toca hablar" sale gratis.

- [ ] **Step 4: Run full test suite**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/ -v`
Expected: PASS todos (65 antiguos + los nuevos de Tasks 1-6).

- [ ] **Step 5: Smoke test integrado (manual, con dicta corriendo)**

1. Arrancar dicta: `X:\Proyectos\dicta\.venv\Scripts\pythonw.exe -m dicta` (o `python.exe -m dicta` para ver la consola). Esperar "Voz lista." (primera vez descarga ~310 MB).
2. En PowerShell, simular un cierre con pregunta:
```powershell
'{"last_assistant_message":"Hecho.\n\n¿Quieres que haga commit?"}' | powershell -NoProfile -File X:\Proyectos\dicta\hooks\stop.ps1
```
Expected: en ≤1 s dicta pasa a cápsula marfil/tinta, **se oye "¿Quieres que haga commit?"**, suena el ding y queda escuchando (cápsula terracota); decir "no, espera" → se pega en la terminal activa. Repetir con un cierre sin pregunta (`"Hecho. Todo en verde."`) → habla y vuelve a reposo sin abrir mic.
3. Click durante la lectura → corta la voz y abre escucha.
4. Check "Voz" del menú desmarcado → los mensajes nuevos no suenan.

- [ ] **Step 6: Commit**

```bash
git add dicta/app.py
git commit -m "feat: cablear la voz de salida (cola, speaker y auto-escucha)"
```

---

### Task 9: Documentación y cierre

**Files:**
- Modify: `INSTALL.md` (registrar los hooks nuevos en settings.json, junto a los SessionStart/End existentes)
- Modify: `README.md` (qué hace la voz de salida + limitación AskUserQuestion)
- Modify: `docs/manual-test-checklist.md` (checklist de la spec)

**Interfaces:** ninguna (solo docs).

- [ ] **Step 1: INSTALL.md**

Localizar la sección donde se registran los hooks `SessionStart`/`SessionEnd` en `~/.claude/settings.json` y añadir los dos nuevos con el mismo formato/estilo que use esa sección, apuntando a `hooks\notification.ps1` y `hooks\stop.ps1` del clon. Contenido de referencia (adaptar al formato exacto que ya use INSTALL.md):

```json
"Notification": [
  { "hooks": [ { "type": "command", "command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"<RUTA_DEL_CLON>\\hooks\\notification.ps1\"" } ] }
],
"Stop": [
  { "hooks": [ { "type": "command", "command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"<RUTA_DEL_CLON>\\hooks\\stop.ps1\"" } ] }
]
```

Añadir también: la primera ejecución descarga el modelo de voz (~310 MB) a `%APPDATA%\dicta\models\kokoro\`, y la referencia de la sección `[voz]` del config (copiar el bloque de `config.example.toml` de Task 4 con una línea de explicación por campo).

- [ ] **Step 2: README.md**

Añadir a la lista de features: "Voz de salida (v3): dicta lee los avisos y el cierre de cada respuesta de Claude con TTS 100% local (Kokoro), y si termina en pregunta abre el micrófono para que contestes". Documentar la limitación: las preguntas con formulario de opciones (AskUserQuestion) no exponen su texto a los hooks del CLI y no se leen.

- [ ] **Step 3: manual-test-checklist.md**

Añadir sección "v3 — voz de salida" con estos ítems (copiados de la spec):

```markdown
## v3 — voz de salida

- [ ] Respuesta de Claude que termina en pregunta → dicta la lee → ding →
      contestas → aparece en la terminal con Enter.
- [ ] Respuesta sin pregunta → dicta lee el cierre y vuelve a reposo sin abrir mic.
- [ ] Aviso de permiso → lo lee, NO abre mic.
- [ ] Click durante la lectura corta la voz y abre escucha.
- [ ] El TTS diciendo "Claude" no dispara el wake word.
- [ ] Check "Voz" del menú apaga/enciende al vuelo.
- [ ] Validar la voz ef_dora con los oídos de Robert; si no convence, plan B Piper.
```

- [ ] **Step 4: Suite completa y commit final**

Run: `X:\Proyectos\dicta\.venv\Scripts\python.exe -m pytest tests/ -v`
Expected: PASS todos.

```bash
git add INSTALL.md README.md docs/manual-test-checklist.md
git commit -m "docs: instalacion y checklist de la voz de salida (v3)"
```

---

## Self-review del plan (hecho al escribirlo)

- **Cobertura del spec:** hooks (T7), cola+coalescing (T2, T8), speaker Kokoro con `ensure_model` (T6), estado SPEAKING con las 4 transiciones de la tabla (T3), widget+menú (T5), config `[voz]` (T4), extracción/heurística (T1), docs+checklist (T9). Manejo de errores: voz OFF si Kokoro falla (T8 `load_voz`), fallo de audio → `on_done` siempre (T6), corruptos borrados (T2), hooks que nunca fallan (T7).
- **Sin placeholders:** cada step lleva código o comando concreto. Los dos puntos abiertos están marcados como verificación explícita, no como TODO: el código de idioma de kokoro-onnx (T6 Step 5) y el formato exacto de la sección de hooks de INSTALL.md (T9 Step 1, con contenido de referencia).
- **Consistencia de tipos:** `preparar(...) -> list[tuple[str, bool]]` (T2) es lo que consume `pendientes` (T8); `speak_request(abre_mic) -> bool` (T3) se usa igual en T8; `Speaker(synth, player, on_level, on_done)` (T6) casa con `crear_speaker(cfg, MODELS_DIR, bridge.speak_level.emit, bridge.speak_done.emit)` (T8); `voice_toggled`/`set_voice` (T5) casan con T8.
